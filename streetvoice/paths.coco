from pathlib import Path

root = Path(__file__).parent

db_file = root / 'dumpfile.db'
assets_dir = root / 'assets'
output_dir = root / 'output'

songs_file = assets_dir / 'songs.json'
songs_extra_file = assets_dir / 'songs_extra.json'

def get_ts_file(song_id) = assets_dir / f"{song_id}.ts"
